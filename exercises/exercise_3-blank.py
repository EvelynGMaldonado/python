'''

EXERCISE #3: CREATE A CONDITIONAL BOOLEAN EXPRESSION POTENTIALLY MUTUALLY EXCLUSIVE that determines the grade of a student dependind on their inputed score.

    Interview/Test Question might be something like --> IMPLEMENT A CONDITIONAL BOOLEAN EXPRESSION POTENTIALLY MUTUALLY EXCLUSIVE using the keywords: and, if, elif, else

    a) define a main function and call it at the end of the file.
    b) creating any variables into my main() function that I might need to store date
    c) call any needed functions into my main() function, using conditionals if needed.
    d) define any functions used into the main function right before calling the main function at the end of the page.

    *prompt the user to its score
    *depending on the inputed score, their correct grade will be returned

'''